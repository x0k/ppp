---
title: Платежные системы
---
import Problem from "@/containers/problem.astro";
import Hint from "@/components/hint.astro";
import { ProblemCategory } from '@/shared/problems';

{/* <Problem category={ProblemCategory.DesignPatterns}>
  # Платежные системы

  Необходимо выполнить интеграцию с тремя платежными системами, реализовав операцию `payment`.

  TODO: пояснения к `payment`, `base` и `amount`.

  Требования к реализации операции `payment`:

  - PayPal - увеличивает `base` на `amount`
  - WebMoney - увеличивает `base` на `2*amount`
  - BankCat - увеличивает `base` в `amount` раз

  > Для тестовых данных результат операций будет целочисленным положительным числом.

  <Hint title="Рекомендуемый паттерн проектирования">Используй фабрику.</Hint>

  <Editor slot="editor" />
</Problem> */}
Bar content
